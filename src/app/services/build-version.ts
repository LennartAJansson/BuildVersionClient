// Minimal re-export surface kept for transitional compatibility. No dynamic discovery logic remains.
export { BuildVersionService } from './build-version.service';
export type { BuildVersion as BuildVersionResponse } from './build-version.types';
